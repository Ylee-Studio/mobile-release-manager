workflow:
  timezone: "Europe/Moscow"
  heartbeat:
    active_minutes: 15
    idle_minutes: 240
  storage:
    memory_db_path: "artifacts/crewai_memory.db"
    audit_log_path: "artifacts/workflow_audit.jsonl"
    slack_outbox_path: "artifacts/mock_slack_outbox.jsonl"
    slack_events_path: "artifacts/mock_slack_events.jsonl"
    jira_outbox_path: "artifacts/mock_jira_outbox.jsonl"
  readiness_owners:
    Growth: "Мурат Камалов"
    Core: "Антон Давыдов"
    AutoCut: "Кирилл Горбачёнок"
    AI Tools: "Макс Адамян"
    Activation: "Евгений Кулинич"
    Влит релиз Движка в dev: "Павел Кряжевских"
    Влит релиз FeaturesKit в dev: "Кирилл Горбачёнок"

slack:
  channel_id: "${SLACK_ANNOUNCE_CHANNEL}"

jira:
  project_keys:
    - "CTV"
    - "CORE"
    - "VALUE"
    - "AC"
    - "IACTEAM"

retry:
  max_attempts: 3
  base_delay_seconds: 0.5
  max_delay_seconds: 2.0
