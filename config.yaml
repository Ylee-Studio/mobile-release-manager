workflow:
  timezone: "Europe/Moscow"
  storage:
    memory_db_path: "artifacts/workflow_state.jsonl"
    audit_log_path: "artifacts/workflow_audit.jsonl"
    slack_events_path: "artifacts/slack_events.jsonl"
  readiness_owners:
    Growth: "<@U09TJ0GUW4D>" # Мурат Камалов
    Core: "<@U05JPDRJR44>" # Антон Давыдов
    AutoCut: "<@U06U3KAELCU>" # Кирилл Горбачёнок
    AI Tools: "<@U07MT1SK3K2>" # Макс Адамян
    Activation: "<@U09ND68KZ9D>" # Евгений Кулинич
    Влит релиз Движка в dev: "<@U043XSCKZ0V>, <@U044DSV5H1P>" # Павел Кряжевских, Антон Чунарев
    Влит релиз FeaturesKit в dev: "<@U06U3KAELCU>" # Кирилл Горбачёнок

slack:
  channel_id: "${SLACK_ANNOUNCE_CHANNEL}"
  bot_token: "${SLACK_BOT_TOKEN}"

jira:
  project_keys:
    - "CTV"
    - "CORE"
    - "VALUE"
    - "AC"
    - "IACTEAM"

github:
  repo: "${GITHUB_REPO}"
  token: "${GITHUB_TOKEN}"
  workflow_file: "create_release_branch.yml"
  ref: "main"
  poll_interval_seconds: 30

retry:
  max_attempts: 3
  base_delay_seconds: 0.5
  max_delay_seconds: 2.0
